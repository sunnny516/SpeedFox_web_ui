// 在加速器启动的时候添加我的签名和感谢语


function jsrun() {
    // 创建一个新的img元素
    const img = document.createElement('img');

    // 设置图片的src属性
    img.src = 'https://api.jihujiasuqi.com/up_img/other/qianm.png';

    // 添加样式
    img.style.width = '500px';
    img.style.height = 'auto';
    img.style.filter = ' brightness(10000%);';
    img.style.margin = '0 0 0 -510px';
    img.style.position = 'relative';
    // 将图片添加到指定的容器中
    // document.body.appendChild(img);
}